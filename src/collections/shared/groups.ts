// Sidebar groups for the admin panel. Groups appear in the nav in the order
// their first member is declared in `payload.config.ts` (collections first,
// then globals), so keep the collection order in that file in step with this.
export const CONTENT_GROUP = { en: 'Content', ro: 'Conținut' }
export const SCHEDULE_GROUP = { en: 'Schedule & locations', ro: 'Program & locații' }
export const SYSTEM_GROUP = { en: 'System', ro: 'Sistem' }
export const SETTINGS_GROUP = { en: 'Configuration', ro: 'Configurare' }
