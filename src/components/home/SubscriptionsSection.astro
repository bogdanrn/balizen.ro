---
import Image from '../common/Image.astro';
import type { Action, SubscriptionHighlight } from '../../types';
import { cn } from '../../utils/cn';

const { subscriptions, action } = Astro.props as {
  subscriptions: SubscriptionHighlight[];
  action: Action;
};
---

<section id="abonamente" class="bg-white">
  <div class="mx-auto max-w-screen-xl px-4 py-24 sm:px-6 lg:px-8">
    <div class="mx-auto max-w-2xl text-center">
      <p class="text-sm font-semibold uppercase tracking-[0.35em] text-primary">Abonamente</p>
      <h2 class="mt-4 font-heading text-3xl font-bold text-slate-900 sm:text-4xl">Prețuri și pachete Bali Zen</h2>
      <p class="mt-3 text-base text-slate-600">
        Alege abonamentul care se potrivește cel mai bine ritmului tău și bucură-te de relaxare autentic balineză, în
        mod constant.
      </p>
    </div>

    <div class="mt-16 space-y-16">
      {
        subscriptions.map((subscription, index) => {
          const isReversed = index % 2 === 1;

          return (
            <article
              class="grid gap-10 rounded-3xl border border-slate-200 bg-slate-50/60 p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-lg lg:grid-cols-[0.6fr_0.4fr]"
              id={subscription.id}
            >
              <div class="space-y-6 self-center">
                <div class="flex items-center gap-3 text-xs font-semibold uppercase tracking-[0.35em] text-primary">
                  <span>Abonament</span>
                </div>
                <h3 class="font-heading text-2xl font-semibold text-slate-900">{subscription.title}</h3>
                <p class="text-sm leading-relaxed text-slate-600">{subscription.summary}</p>

                <ul class="space-y-3 text-sm text-slate-700">
                  {subscription.highlights.map((highlight) => (
                    <li class="flex items-start gap-3">
                      <span class="mt-1 inline-flex h-6 w-6 shrink-0 items-center justify-center rounded-full bg-primary/10 text-primary">
                        <svg class="h-3 w-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
                          <path d="M5 13l4 4L19 7" stroke-linecap="round" stroke-linejoin="round" />
                        </svg>
                      </span>
                      <span>{highlight}</span>
                    </li>
                  ))}
                </ul>

                <div>
                  <a
                    href={action.href}
                    class={cn(
                      'inline-flex items-center gap-2 rounded-full bg-primary px-6 py-3 text-sm font-semibold text-white shadow-lg shadow-primary/30 transition hover:opacity-90',
                      action.class
                    )}
                    target={action.target}
                    rel="noopener"
                  >
                    {action.label}
                  </a>
                </div>
              </div>

              <Image
                src={subscription.image.src}
                alt={subscription.image.alt}
                class={`h-full w-full rounded-3xl object-cover shadow-md shadow-slate-200 ${isReversed ? 'lg:order-first' : ''}`}
                layout="responsive"
                sizes="(min-width: 1024px) 35vw, 90vw"
              />
            </article>
          );
        })
      }
    </div>
  </div>
</section>
