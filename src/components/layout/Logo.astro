---
import logoColor from '../../assets/logo/balizen_logo_color.png';
import { getLangFromUrl, getLocalizedPath } from '../../i18n/utils';

const lang = getLangFromUrl(Astro.url);
---

<a href={getLocalizedPath('/', lang)} class="inline-flex items-center gap-3" aria-label={lang === 'ro' ? 'Bali Zen acasă' : 'Bali Zen home'}>
  <img src={logoColor.src} alt="Sigla Bali Zen" class="h-14 w-14 object-contain" loading="eager" decoding="async" />
  <div class="flex-col text-left leading-tight flex">
    <span class="font-heading text-xl font-semibold text-slate-900">Bali Zen</span>
    <span class="hidden text-xs font-medium uppercase tracking-[0.35em] text-slate-500 custom-xl:inline">Masaj Autentic Balinez</span>
  </div>
</a>
